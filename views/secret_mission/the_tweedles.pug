html
	head
		title The Tweedles
		include ../JADE-Bootstrap/_bootstrap
		include ../mixins.pug
		link(rel='stylesheet', type="text/css", href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css')
		link(rel='stylesheet', type="text/css", href='/css/styles.css')

		script(type="text/javascript").
			function moveOn() {
				return true;
			}
	body.secret_mission
		p What would you like to do?

		form(action='/secret_mission/the_tweedles/tweedle1', method="POST")
			input.hidden(type="hidden", name="name", value=name)
			div(align="center")
				button.navigate(onclick="return moveOn()") Talk to Tweedle 1

		form(action='/secret_mission/the_tweedles/tweedle2', method="POST")
			input.hidden(type="hidden", name="name", value=name)
			div(align="center")
				button.navigate(onclick="return moveOn()") Talk to Tweedle 2

		form(action='/secret_mission/the_tweedles/tweedle3', method="POST")
			input.hidden(type="hidden", name="name", value=name)
			div(align="center")
				button.navigate(onclick="return moveOn()") Talk to Tweedle 3

		form(action='/secret_mission/password_door', method="POST")
			input.hidden(type="hidden", name="name", value=name)
			div(align="center")
				button.navigate(onclick="return moveOn()") Try the password

		br

		p *Note: This first challenge is shared by all agents (ie the answer will be the same for everyone). Each agent will be given their own clue and you will have to piece together the clues and work together to figure out the passwords.

		p All future challenges, however, are specific to the individual. The concepts for each individual challenge are the same across the board though, so if you need help on your individual challenge, ask your teammates!
