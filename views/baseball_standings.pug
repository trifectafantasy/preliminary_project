html
	head
		title= year + " Baseball Standings"
		link(rel="icon", type="image/x-icon", href="/images/trifectalogo.ico")
		include ./JADE-Bootstrap/_bootstrap
		include mixins.pug
		link(rel='stylesheet', type="text/css", href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css')
		link(rel='stylesheet', type="text/css", href='/css/styles.css')

		script(type="text/javascript").

			$(window).on('beforeunload', function() {
				$(".loader").fadeIn("slow");
				//console.log("UNLOAD");
			})

			$(window).load(function() {
				$(".loader").fadeOut("slow");
				//console.log("LOAD");
			})

	body
		div.loader
		header
			+headliner


		h2= year + " Baseball Standings"

		if playoffs === true

			h4= "Trifecta Standings"
			table
				thead
					tr
						th TEAM
						th H2H TRIFECTA POINTS
						th ROTO TRIFECTA POINTS
						th REGULAR SEASON TRIFECTA POINTS
						th PLAYOFF TRIFECTA POINTS
						th TOTAL TRIFECTA POINTS
				tbody
					each readin2 in trifecta_standings
						tr
							each val, cat in readin2
								if cat === "team"
									td= val
								if cat === "h2h_trifecta_points"
									td= val
								if cat === "roto_trifecta_points"
									td= val
								if cat === "season_trifecta_points"
									td= val
								if cat === "playoff_trifecta_points"
									td= val
								if cat === "total_trifecta_points"
									td= val

			h4= "Head to Head Standings"
			table
				thead
					tr
						th TEAM
						th WINS
						th LOSSES
						th TIES
						th WIN %
						th DIVISION
						th H2H TRIFECTA POINTS
				tbody
					each readin in h2h_standings
						tr
							each val, cat in readin
								if cat === "team"
									td= val
								if cat === "wins"
									td= val
								if cat === "losses"
									td= val
								if cat === "ties"
									td= val
								if cat === "win_per"
									td= parseFloat(val).toFixed(3)
								if cat === "division"
									td= val
								if cat === "h2h_trifecta_points"
									td= val

			h4= "Roto Points per Category"
			table
				thead
					tr
						th TEAM
						th R
						th HR
						th RBI
						th SO
						th SB
						th OBP
						th K 
						th QS
						th W
						th SV
						th ERA
						th WHIP
						th TOTAL ROTO POINTS
						th ROTO TRIFECTA POINTS
				tbody
					each next in roto_standings
						tr
							each val, cat in next
								if cat === "team"
									td= val
								if cat === "category_points_R"
									td= val
								if cat === "category_points_HR"
									td= val
								if cat === "category_points_RBI"
									td= val
								if cat === "category_points_SO"
									td= val
								if cat === "category_points_SB"
									td= val
								if cat === "category_points_OBP"
									td= val
								if cat === "category_points_K"
									td= val
								if cat === "category_points_QS"
									td= val
								if cat === "category_points_W"
									td= val
								if cat === "category_points_SV"
									td= val
								if cat === "category_points_ERA"
									td= val
								if cat === "category_points_WHIP"
									td= val
								if cat === "total_roto_points"
									td= val
								if cat === "roto_trifecta_points"
									td= val

			h4= "Roto Stats"
			table
				thead
					tr
						th TEAM
						th R
						th HR
						th RBI
						th SO
						th SB
						th OBP
						th K 
						th QS
						th W
						th SV
						th ERA
						th WHIP
				tbody
					each next in roto_standings
						tr
							each val, cat in next
								if cat === "team"
									td= val
								if cat === "R"
									td= val
								if cat === "HR"
									td= val
								if cat === "RBI"
									td= val
								if cat === "SO"
									td= val
								if cat === "SB"
									td= val
								if cat === "OBP"
									td= parseFloat(val).toFixed(4)
								if cat === "K"
									td= val
								if cat === "QS"
									td= val
								if cat === "W"
									td= val
								if cat === "SV"
									td= val
								if cat === "ERA"
									td= parseFloat(val).toFixed(3)
								if cat === "WHIP"
									td= parseFloat(val).toFixed(3)


		else

			h4= "Head to Head Standings"
			table
				thead
					tr
						th TEAM
						th WINS
						th LOSSES
						th TIES
						th WIN %
						th DIVISION
						th H2H TRIFECTA POINTS
				tbody
					each readin in h2h_standings
						tr
							each val, cat in readin
								if cat === "team"
									td= val
								if cat === "wins"
									td= val
								if cat === "losses"
									td= val
								if cat === "ties"
									td= val
								if cat === "win_per"
									td= parseFloat(val).toFixed(3)
								if cat === "division"
									td= val
								if cat === "h2h_trifecta_points"
									td= val

			h4= "Roto Points per Category"
			table
				thead
					tr
						th TEAM
						th R
						th HR
						th RBI
						th SO
						th SB
						th OBP
						th K 
						th QS
						th W
						th SV
						th ERA
						th WHIP
						th TOTAL ROTO POINTS
						th ROTO TRIFECTA POINTS
				tbody
					each next in roto_standings
						tr
							each val, cat in next
								if cat === "team"
									td= val
								if cat === "category_points_R"
									td= val
								if cat === "category_points_HR"
									td= val
								if cat === "category_points_RBI"
									td= val
								if cat === "category_points_SO"
									td= val
								if cat === "category_points_SB"
									td= val
								if cat === "category_points_OBP"
									td= val
								if cat === "category_points_K"
									td= val
								if cat === "category_points_QS"
									td= val
								if cat === "category_points_W"
									td= val
								if cat === "category_points_SV"
									td= val
								if cat === "category_points_ERA"
									td= val
								if cat === "category_points_WHIP"
									td= val
								if cat === "total_roto_points"
									td= val
								if cat === "roto_trifecta_points"
									td= val

			h4= "Roto Stats"
			table
				thead
					tr
						th TEAM
						th R
						th HR
						th RBI
						th SO
						th SB
						th OBP
						th K 
						th QS
						th W
						th SV
						th ERA
						th WHIP
				tbody
					each next in roto_standings
						tr
							each val, cat in next
								if cat === "team"
									td= val
								if cat === "R"
									td= val
								if cat === "HR"
									td= val
								if cat === "RBI"
									td= val
								if cat === "SO"
									td= val
								if cat === "SB"
									td= val
								if cat === "OBP"
									td= parseFloat(val).toFixed(4)
								if cat === "K"
									td= val
								if cat === "QS"
									td= val
								if cat === "W"
									td= val
								if cat === "SV"
									td= val
								if cat === "ERA"
									td= parseFloat(val).toFixed(3)
								if cat === "WHIP"
									td= parseFloat(val).toFixed(3)
